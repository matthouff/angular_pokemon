import { PokemonTypesColorPipe } from './pokemon-types-color.pipe';

describe('PokemonTypesColorPipe', () => {
  it('create an instance', () => {
    const pipe = new PokemonTypesColorPipe();
    expect(pipe).toBeTruthy();
  });
});
